streamlit>=1.28.0,<2.0.0

# mediapipe 0.10.14-0.10.21 still exposes mp.solutions.hands
mediapipe>=0.10.14,<0.10.22
opencv-python-headless>=4.8.0

# numpy 2.0+ required so pickle can load the model
numpy>=2.0.0

scikit-learn>=1.3.0

# Keep the WebRTC stack reproducible on Streamlit Cloud.
streamlit-webrtc==0.64.6
aiortc==1.14.0
aioice==0.10.2
av==16.1.0
tornado
